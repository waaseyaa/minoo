# Minoo test environment overrides
#
# Loaded automatically by Symfony Dotenv when APP_ENV=testing.
# Disables dev fallback account so Playwright tests exercise real auth flows.

APP_ENV=testing
APP_DEBUG=true

# Disable dev fallback account — tests must use real auth
WAASEYAA_DEV_FALLBACK_ACCOUNT=false
