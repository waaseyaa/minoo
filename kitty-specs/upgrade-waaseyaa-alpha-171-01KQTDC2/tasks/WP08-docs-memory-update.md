---
work_package_id: WP08
title: Docs and memory pointers
dependencies:
- WP07
requirement_refs:
- FR-006
- FR-009
planning_base_branch: main
merge_target_branch: main
branch_strategy: Direct work on main (after WP07). Documentation-only change.
subtasks:
- T035
- T036
- T037
- T038
history:
- date: '2026-05-04T21:23:06Z'
  action: created
  notes: Generated by spec-kitty.tasks skill.
authoritative_surface: CLAUDE.md
execution_mode: code_change
mission_slug: upgrade-waaseyaa-alpha-171-01KQTDC2
owned_files:
- CLAUDE.md
priority: P2
tags: []
---

# WP08 — Docs and memory pointers

## Objective

Update the CLAUDE.md "Last framework sync" line to alpha.171, refresh the Minoo MEMORY.md framework version pointer, and confirm `ComposerProviderParityTest` still passes against the new package manifest.

## Context

- CLAUDE.md currently reads: "Last framework sync: Waaseyaa alpha.157 (HTTP boot-failure JSON hints; skeleton index Dotenv default; composer waaseyaa/* bump)"
- MEMORY.md (auto-memory) carries: "Framework at alpha.107 — upgraded 2026-04-06 during incident recovery". This is stale and inconsistent with both CLAUDE.md and reality (we found alpha.143 installed). Per Claudia principles, MEMORY.md should not carry volatile facts; replace with a pointer to composer.lock as canonical.
- `ComposerProviderParityTest` asserts the 5 composer providers in `extra.waaseyaa.providers` match the trailing segment of the compiled package manifest. If the framework bump altered the manifest format, this test catches it.

## Branch Strategy

Direct work on `main` (after WP07 confirms smoke tests pass). No worktree.

## Subtasks

### T035 — Update CLAUDE.md "Last framework sync" line

**Purpose**: Reflect the new framework state in the Tier-1 constitution.

**Steps**:
1. Edit CLAUDE.md. Find the line: `Last framework sync: Waaseyaa alpha.157 (...)`.
2. Replace with: `Last framework sync: Waaseyaa alpha.171 (FieldStorage _data symmetry, schema-drift diagnostics, kernel-subclass test ban; composer waaseyaa/* bump)`.
3. If the parenthetical needs trimming, focus on the changes that actually drove Minoo-side fixes (those discovered in WP02–WP04).

**Files**: `CLAUDE.md` (one line).

**Validation**:
- `grep -n "Last framework sync" CLAUDE.md` shows alpha.171.

### T036 — Update Minoo MEMORY.md framework version pointer

**Purpose**: Replace the stale "alpha.107" snapshot with a pointer-shaped reference per the data-freshness rule.

**Steps**:
1. Open `/home/jones/.claude/projects/-home-jones-dev-minoo/memory/MEMORY.md`.
2. Find the line: "Framework at alpha.107 — upgraded 2026-04-06 during incident recovery (PR #632...)..."
3. Replace with a pointer-shaped fact: "Framework version is canonical in `composer.lock`. As of 2026-05-04 the upgrade mission `upgrade-waaseyaa-alpha-171-01KQTDC2` brought all waaseyaa/* packages to alpha.171."
4. Do NOT delete the surrounding context paragraph entirely — keep the project-state highlights but strip the volatile counts.

**Files**: `/home/jones/.claude/projects/-home-jones-dev-minoo/memory/MEMORY.md`.

**Validation**:
- The MEMORY.md framework line now contains a pointer ("composer.lock") rather than a hard-coded version.

### T037 — Verify ComposerProviderParityTest still passes

**Purpose**: Confirm the composer provider list still matches the compiled package manifest after the upgrade.

**Steps**:
1. Run: `./vendor/bin/phpunit --filter ComposerProviderParityTest`.
2. Expected: pass.
3. If failure, the framework alpha.144–171 changed how providers are compiled; investigate `extra.waaseyaa.providers` in `composer.json` versus the manifest in `vendor/composer/installed.json` or wherever the compiler reads from.

**Validation**:
- `./vendor/bin/phpunit --filter ComposerProviderParityTest` exits 0.

### T038 — Commit docs / memory updates

**Purpose**: Land the documentation change.

**Steps**:
1. `git add CLAUDE.md`.
2. The MEMORY.md edit is outside the repo (in the auto-memory directory) — not committed. Verify it was edited.
3. Commit with message:
   ```
   docs: bump framework sync line to alpha.171

   Reflects the upgrade mission completion and replaces the stale
   alpha.157 reference. MEMORY.md framework pointer updated separately
   (auto-memory location).
   ```

**Validation**:
- `git status` clean for CLAUDE.md.
- MEMORY.md outside the repo reflects the new pointer.

## Definition of Done

- [ ] CLAUDE.md "Last framework sync" line shows alpha.171.
- [ ] MEMORY.md carries a pointer to composer.lock, not a hardcoded version.
- [ ] `ComposerProviderParityTest` passes.
- [ ] CLAUDE.md change committed.

## Risks

- **MEMORY.md drift accumulates**: This is the third version mentioned in MEMORY.md (alpha.107, then implicit alpha.143/.157 in CLAUDE.md, now alpha.171). The pointer-shape fix is meant to stop this drift class. If pushback, defer the MEMORY.md edit but log a follow-up to address it.
- **Provider parity break**: If the parity test fails, do NOT silently update the test — investigate whether the framework changed the manifest contract.

## Reviewer Guidance

- Confirm the parenthetical in the sync line names the *consequential* changes that affected Minoo, not a verbatim release-note dump.
- Confirm the MEMORY.md edit is pointer-shaped, not "alpha.171 as of 2026-05-04 the next stale value".

## Implementation command

```bash
spec-kitty agent action implement WP08 --agent <name>
```
