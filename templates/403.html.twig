{% extends "base.html.twig" %}

{% block title %}{{ status_code }} {{ title }} — Minoo{% endblock %}

{% block content %}
  <div class="flow-lg">
    <h1>{{ title }}</h1>
    <p>{{ detail }}</p>
    <p><a href="/login?redirect={{ request_path|url_encode }}">Sign in</a> to access this page, or <a href="/">return to the homepage</a>.</p>
  </div>
{% endblock %}
